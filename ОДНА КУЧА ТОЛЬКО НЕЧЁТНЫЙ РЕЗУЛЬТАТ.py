# Отличие — каждый ход доступен только если результат будет нечётным
def f(s,m):
    if s>=51: return m%2==0   #менять 51 — граница
    if m==0: return 0          #не трогать
    h = []                     #не трогать
    if (s+1)%2!=0: h+=[f(s+1,m-1)]   #ход +1 только если результат нечётный
    if (s+3)%2!=0: h+=[f(s+3,m-1)]   #ход +3 только если результат нечётный
    if s*3%2!=0:   h+=[f(s*3,m-1)]   #ход ×3 только если результат нечётный
    #менять +1, +3, *3 на ходы из задачи
    #ВАЖНО: условие (s+1)%2!=0 означает "результат нечётный"
    #если нужно чётный результат — меняй !=0 на ==0
    return any(h) if m%2!=0 else all(h)  #не трогать
    # ЕСЛИ В ВОПРОСЕ 19 написано "после неудачного хода" — меняй all(h) на any(h) ТОЛЬКО для 19,
    # запускай, смотришь ответ, потом ВОЗВРАЩАЙ all(h) обратно и запускай 20 и 21

print('19',[s for s in range(1,51) if f(s,2)])                        #менять только 51
print('20',[s for s in range(1,51) if not f(s,1) and f(s,3)])         #менять только 51
print('21',[s for s in range(1,51) if not f(s,2) and f(s,4)])         #менять только 51
